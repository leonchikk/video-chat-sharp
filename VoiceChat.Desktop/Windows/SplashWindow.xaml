<Window x:Class="VoiceChat.Desktop.Windows.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VoiceChat.Desktop.Windows"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        xmlns:viewmodels="clr-namespace:VoiceChat.Desktop.ViewModels" 
        ResizeMode="NoResize"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Background="#36393f"
        Height="200" Width="400"
        Loaded="Window_Loaded"
        Unloaded="Window_Unloaded"
       >
    <Grid>
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="Loaded">
                <i:InvokeCommandAction Command="{Binding ConnectCommand}" />
            </i:EventTrigger>
        </i:Interaction.Triggers>
        <Label Content="MadJuns" Foreground="White" FontSize="48" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Label Content="v0.01" Foreground="White" FontSize="12" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="352,164,0,0"/>
    </Grid>
</Window>
